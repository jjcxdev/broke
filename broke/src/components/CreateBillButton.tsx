import React from "react";

const CreateBillButton: React.FC = () => {
  return (
    <button className="flex items-center justify-center rounded-full bg-pink-400 p-2 hover:bg-neutral-500">
      Create New Bill
    </button>
  );
};

export default CreateBillButton;
